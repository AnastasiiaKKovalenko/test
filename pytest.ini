[pytest]
markers = 
    change: Tests that modify name of a user
    check: Tests that check user name
    http: Tests that send http requests
    api: Tests that check user information from response on the GitHub server.